node_modules/
main.js
*.log
.DS_Store

# Local dev tools / specs — contain absolute paths and are not part of the
# distributable plugin. Kept locally only.
scripts/
# Ignore docs/ contents (local notes w/ absolute paths) EXCEPT the
# version-controlled design specs. Git can't re-include a path whose parent dir
# is excluded, so exclude docs/* (one level) not docs/.
# Re-include every version spec dir (docs/<x.y.z>/, name starts with a digit) so
# new releases are tracked automatically without editing this file each bump.
# docs/old/ stays ignored (archived stale notes); docs/superpowers/ kept too.
docs/*
!docs/superpowers/
!docs/[0-9]*/

# Disposable debug/experiment scripts. Put one-off scratch here so the test/ root
# stays clean; reusable CDP E2E harnesses live in test/e2e/ and ARE tracked.
test/scratch/

# Image artifacts written to the repo root by scratch render/preview scripts.
stage1-mesh.svg
stage1-mesh.png
mvexact.png
mvplain.png
mvfine.png
mvobl.png
tex-verify.png
logGrid-full.png
stage1-plugin.png
stage1-plugin.svg
soft-test.png
soft-full.png
fwd-grid.png
fwd-grid.svg
vec-test.png
vec-tile.png
power-test.png
cheby-test.png
mip-test.png
scallop-test.png
own-tile-test.png
fwd-conformal.png
fwd-conformal.svg
fwd-conf2.png
fwd-conf2.svg
preview5.png
preview5.svg
preview5b.png
preview5b.svg
preview-nest2.png
preview-nest2.svg
preview-nest5.png
preview-nest5.svg
preview-chain.png
preview-chain.svg
preview-zoom0.png
preview-zoom0.svg
preview-zoom05.png
preview-zoom05.svg
lens-*.png
lens-*.svg
gallery-*.png
hive-*.png

# Playwright MCP browser snapshot artifacts
.playwright-mcp/

# Misc scratch artifacts left by E2E + UI tooling (Kaizen cleanup)
ui-tester-log.txt
capture-toggles.js
update_dev_vault.py
